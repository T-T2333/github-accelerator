{
  "name": "echomusic-github-accelerator",
  "version": "1.1.1",
  "private": true,
  "description": "EchoMusic 插件：GitHub 公益加速源选择与测速",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "test": "node scripts/test-plugin.mjs",
    "sync:mirrors": "node scripts/sync-mirrors.mjs",
    "check:mirrors": "node scripts/sync-mirrors.mjs --check",
    "probe:mirrors": "node scripts/probe-mirrors.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
